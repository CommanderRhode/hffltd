var requestActivator = require('../server/requestActivator.js');

describe("Making sure requestActivator behaves", function() {
  it("when a known file is passed in", function() {
    expect(true).toBe(true);
  });
});